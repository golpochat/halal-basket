{
  "name": "frontend-retired",
  "private": true,
  "version": "0.1.0",
  "description": "RETIRED — use frontend-customer / frontend-shop / frontend-driver",
  "scripts": {
    "dev": "node -e \"console.error('RETIRED: use npm run dev:customer (or dev:shop / dev:driver)'); process.exit(1)\"",
    "build": "node -e \"console.error('RETIRED: use frontend-customer / frontend-shop / frontend-driver'); process.exit(1)\""
  }
}
